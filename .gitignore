.pio/
.vscode/
.DS_Store
data/

# Python (linux-bridge)
__pycache__/
*.pyc
.venv/
dist/
*.egg-info/

# Claude Code scratch. .claude/worktrees/ holds entire nested checkouts of this
# repo, so leaving it untracked-but-committable means one `git add -A` commits a
# copy of the repo into itself. settings.local.json is personal, not team config.
.claude/
.superpowers/
